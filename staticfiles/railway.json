{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn mysite.wsgi --bind 0.0.0.0:$PORT --timeout 120 --workers 2",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
```

Dan **ubah `Procfile` menjadi simple:**
```
web: gunicorn mysite.wsgi --bind 0.0.0.0:$PORT --timeout 120 --workers 2